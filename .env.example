# Backend API base URL (no trailing slash), e.g. https://api.pollolabs.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: canonical site URL used for social-share metadata.
# Falls back to the Vercel deployment URL automatically.
# NEXT_PUBLIC_SITE_URL=https://pollolabs.com
